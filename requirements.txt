# PC Reselling Manager 后端依赖（Python 3.12 / conda 环境 PC_Reselling）
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
python-multipart==0.0.32
PyMySQL==1.2.0
cryptography==46.0.7
PyJWT==2.10.1
bcrypt==5.0.0
requests==2.32.3

# 打包成 exe 后的桌面外壳：托盘图标（pystray）+ 它的图标解码（Pillow）。
# 开发态跑 main.py 用不到，但打包机上必须装——spec 会收集它们的子模块。
pystray==0.19.5
Pillow==12.3.0
